# Pinned runtime environment.
#
# `pip install -e .` resolves the same dependencies from pyproject.toml with looser bounds;
# this file pins the exact versions the pipeline was developed and tested against, which is
# what a reproducible environment needs.
#
# Versions below were verified by running the test suite and `energy-forecast train --skip-deep`.

numpy==2.2.2
pandas==2.2.3
matplotlib==3.10.7          # >=3.9 required: boxplot `tick_labels`
scikit-learn==1.5.2
pyyaml==6.0.3
joblib==1.5.2
click==8.3.0

# Deep learning. Not pinned to an exact build: the correct wheel depends on your Python
# version and on whether you want the CPU or GPU package. Any 2.16+ release exposes the
# Keras 3 API this package uses.
tensorflow>=2.16

# Development and testing.
pytest==9.0.0
ruff>=0.4
mypy>=1.8

# Only needed to open the report notebook.
notebook>=7.0
ipykernel>=6.0
